resources:
  jobs:
    test_oauth:
      name: test_oauth_new
      tasks:
        - task_key: test_task
          notebook_task:
            notebook_path: /Workspace/Users/arbaaz.suleman@tekizma.com/List Scope
            source: WORKSPACE
          environment_key: Default
      queue:
        enabled: true
      environments:
        - environment_key: Default
          spec:
            environment_version: "5"
      performance_target: PERFORMANCE_OPTIMIZED
